# 提案：添加两篇新的功能文档

## 元数据

| 字段 | 值 |
|------|-----|
| **提案 ID** | `feature-documentation-expansion` |
| **状态** | ✅ 执行完成 (ExecutionCompleted) |
| **优先级** | 中 |
| **复杂度** | 低 |
| **预计影响范围** | 文档内容、导航配置 |

---

## 概述

为 SuperDucky.docs 项目添加两篇新的功能文档：

1. **启用 NuGet 功能** - 指导用户如何在 SuperDucky 中启用和使用 NuGet 包管理功能
2. **启用存档图鉴功能** - 指导用户如何启用和使用游戏存档的图鉴收集功能

这两篇文档将使用项目中已准备好的截图素材，以图文并茂的方式帮助用户快速上手这两个核心功能。

---

## 背景

SuperDucky.docs 是基于 Docusaurus 3.9.2 构建的官方文档网站。项目已准备好两组教程截图：

- **NuGet 功能截图**：`static/img/screenshots/enable_nuget/` 目录（2 张图片）
- **存档图鉴功能截图**：`static/img/screenshots/enable_save_gallary/` 目录（7 张图片）

当前项目只有一个 `installation.md` 安装文档，缺少功能使用类文档。新用户在安装完成后，需要了解如何启用和使用 SuperDucky 的各项功能。

---

## 问题陈述

当前文档存在以下缺失：

| 缺失内容 | 影响 |
|----------|------|
| NuGet 功能启用指南 | 用户无法了解如何启用 NuGet 包管理功能 |
| 存档图鉴功能指南 | 用户无法了解如何启用和使用存档图鉴收集功能 |

这些文档对于新用户入门和功能探索至关重要。

---

## 提议的解决方案

采用**文件夹层级导航**结构，创建"功能指南"分类，将两篇文档组织在该分类下：

### 导航结构

```
📂 安装指南
📂 功能指南
  ├── 📄 启用 NuGet 功能
  └── 📄 启用存档图鉴功能
```

### 1. 创建功能指南分类目录

创建 `docs/feature-guides/` 目录作为功能文档的分类文件夹。

### 2. 创建 `docs/feature-guides/enable-nuget.md`

描述如何启用 NuGet 功能的完整指南。

**内容结构**：
- 功能简介
- 启用步骤（配合截图）
- 使用说明

**使用的截图**：
```
static/img/screenshots/enable_nuget/
├── 001_enable_nuget..png    (418KB)
└── 002_confirm.png           (608KB)
```

### 3. 创建 `docs/feature-guides/enable-save-gallery.md`

描述如何启用存档图鉴功能的完整指南。

**内容结构**：
- 功能简介
- 依赖安装（如需要）
- 启用步骤（配合截图）
- 功能预览
- 图鉴类型说明

**使用的截图**：
```
static/img/screenshots/enable_save_gallary/
├── 001_install_depends_and_enable.png  (311KB)
├── 002_enable_feature.png               (405KB)
├── 003_preview_gallary.png              (1.02MB)
├── 004_compare_items.png                (564KB)
├── 005_simple_item.png                  (861KB)
├── 006_complex_item.png                 (1.10MB)
└── 007_mod_item.png                     (1.85MB)
```

### 4. 创建分类索引页 `docs/feature-guides/_category_.json`

定义"功能指南"分类的显示配置：

```json
{
  "label": "功能指南",
  "position": 2,
  "link": {
    "type": "generated-index",
    "description": "SuperDucky 各项功能的启用和使用指南"
  }
}
```

### 5. 更新导航配置

修改 `sidebars.ts`，使用嵌套结构：

```typescript
export default {
  mainSidebar: [
    'installation',
    {
      type: 'category',
      label: '功能指南',
      link: {
        type: 'generated-index',
        description: 'SuperDucky 各项功能的启用和使用指南',
      },
      items: [
        'feature-guides/enable-nuget',
        'feature-guides/enable-save-gallery',
      ],
    },
  ],
};
```

---

## 技术规格

### 文档格式规范

遵循项目现有规范：

```yaml
---
title: 页面标题
sidebar_position: 排序位置
---

# H1 标题（通常省略，使用 title 替代）

内容...
```

### 图片引用语法

```markdown
![描述文字](/img/screenshots/目录名/文件名.png)
```

### 内容语言

- 主要语言：简体中文（`zh-Hans`）
- 可选：后续添加英文翻译

---

## 实施计划

详见 [tasks.md](./tasks.md)

---

## 验收标准

### 功能验收

- [x] `docs/feature-guides/` 目录已创建
- [x] `docs/feature-guides/enable-nuget.md` 文件已创建
- [x] `docs/feature-guides/enable-save-gallery.md` 文件已创建
- [x] `docs/feature-guides/_category_.json` 分类配置已创建
- [x] `sidebars.ts` 已更新为嵌套结构
- [x] 所有截图正确引用，无断链
- [x] 文档内容完整，步骤清晰
- [x] 侧边栏显示"功能指南"可折叠分类

### 构建验证

- [x] 运行 `yarn build` 成功
- [x] 构建过程中无断链警告
- [x] 本地预览 `yarn start` 页面显示正常

---

## 风险评估

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| 图片路径错误 | 低 | 中 | 构建时验证断链 |
| 内容不准确 | 低 | 中 | 由熟悉功能的人员审核 |
| 翻译缺失 | 低 | 低 | 后续迭代添加英文版 |

---

## 影响范围

| 类型 | 影响描述 |
|------|----------|
| **新增目录** | `docs/feature-guides/` |
| **新增文件** | 2 个 MDX 文档、1 个分类配置 JSON |
| **修改文件** | `sidebars.ts` |
| **静态资源** | 使用已存在的 9 张截图 |
| **构建时间** | 无明显影响 |
| **用户体验** | 提供层级化的功能使用指南 |

---

## 相关资源

- [Docusaurus 官方文档](https://docusaurus.io/)
- 项目现有文档：`docs/installation.md`
- 截图目录：`static/img/screenshots/`

---

## 变更历史

| 日期 | 版本 | 变更说明 |
|------|------|----------|
| 2025-01-07 | 1.0 | 初始提案 |
